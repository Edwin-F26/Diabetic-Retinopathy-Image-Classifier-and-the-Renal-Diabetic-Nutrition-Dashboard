# Copy this file to .env.local and paste in your own key.
# Free signup: https://fdc.nal.usda.gov/api-key-signup
#
# Without a key the app falls back to the shared DEMO_KEY, which allows roughly
# 30 requests per hour per IP address.
VITE_FDC_API_KEY=DEMO_KEY
